# demo-api

Generated by **ApiForge** — production-ready CRUD API.

## Stack

| | |
|---|---|
| Runtime | Node.js Express |
| Database | SQLite |
| Auth | JWT Bearer + bcrypt |
| Docs | Swagger at /api-docs |
| Port | 3000 |

## Tables

- `main.products` (4 columns)

## Quick start

```bash
cp .env.example .env
# edit .env with your database credentials
npm install
npm run dev
```

Health: http://localhost:3000/health
Swagger: http://localhost:3000/api-docs

## Endpoints

| Method | Path | Auth |
|--------|------|------|
| POST | `/auth/login` | No |
| POST | `/auth/register` | No |
| GET | `/productses` | Yes |
| GET | `/productses/:id` | Yes |
| POST | `/productses` | Yes |
| PUT | `/productses/:id` | Yes |
| DELETE | `/productses/:id` | Yes |

## Security

- CORS: **origins**
- Rate limit: **100/min**
- IP allowlist: **all**
- API key: **off**
- Helmet: **on**

## Users table

If you do not already have a users table, run:

```sql
-- Generated by ApiForge
CREATE TABLE IF NOT EXISTS users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT NOT NULL UNIQUE,
  password TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

---
Forged with ApiForge.
